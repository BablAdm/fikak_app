# Pinned to the 1.x line of the official MCP SDK, which exposes the
# mcp.server.fastmcp.FastMCP API this server is built against. This server only
# uses stdio transport, so the HTTP/WebSocket transport advisories below are not
# reachable here, but the floors are still raised past the affected releases.
mcp>=1.27.2,<2.0.0
httpx>=0.27.0
pydantic>=2.5.0
# Defensive floor: mcp's own transitive floor still allows advisory-affected
# python-multipart releases (DoS/parameter-smuggling issues below 0.0.30).
python-multipart>=0.0.30
